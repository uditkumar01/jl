// run the python script
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                def fullPath = "${WORKSPACE}/main.py"
                echo fullPath
            }
        }
    }
}
